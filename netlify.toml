[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  functions = "netlify/functions"
  publish = "dist"
  
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200